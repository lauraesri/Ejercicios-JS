// Calculo del volumen
const volumen = 16*23*13;
console.log('volumen (cm3)', volumen);

// Calculo del area
const area = 13*23 + 2*23*16;
console.log('area (cm2)', area);

// Generalizar la operación
const largo = 16;
const ancho = 23;
const alto = 13;

const volumen2 = largo * ancho * alto;
const area2 = alto * ancho + 2 * ancho * largo;
console.log('volumen (cm3)', volumen2);
console.log('area (cm2)', area2);